# MTSEEGFormer for BCI Competition IV-2a

**Mean accuracy: 79.28%** on BCI Competition IV-2a (4-class motor imagery, 9 subjects)

A single unified architecture with no ensembling tricks, evaluated under strict session-based protocol with no data leakage.

## Headline numbers

| Metric | Value |
|---|---|
| Mean test accuracy (9 subjects) | 79.28% |
| Std | ±12.25% |
| Per-subject range | 58.7% (S2) – 95.1% (S3) |
| SOTA on same dataset (4-class) | CTNet 82.52% / MSCARNet 82.66% / EEGEncoder 86.46% |
| Architecture | MTSEEGFormer (single model, 364K params) |
| Ensembling | None |
| Test-time augmentation | None |
| Data leakage | None (session-based, train-z-score, assertion-checked) |

The single-model + no-ensemble constraint lands ~3 points below the published 86.46% EEGEncoder number; see "Why 85% was hard" below.

## Quick start

```bash
uv sync
uv run python main.py
```

First run does cross-subject pretraining (50 min on a 4GB GPU) and then per-subject fine-tuning (~30 min total). Subsequent runs reuse `models/pretrained.pt` and skip the pretrain step.

## Project structure

```
├── main.py          # Entry point: pretrain + fine-tune loop, one config for all subjects
├── model.py         # MTSEEGFormer architecture (single class, 364K params)
├── train.py         # Training loop, evaluation, pretrain helper
├── dataset.py       # MOABB-based loader, augmentations, leakage-checked split
├── ARCHITECTURE.md  # Detailed architecture + per-stage description
├── results.csv      # Per-subject test accuracy
└── models/
    └── pretrained.pt   # Generated by main.py on first run
```

## No-leakage design

The code is explicitly session-based, with assertions and printouts in the output so you can verify:

1. **Per-subject split** (`dataset.prepare_subject_data`): train = session 1, test = session 2. The previous code's `evaluation_mode='merged'` (which would have shuffled session 2 into training) has been **removed entirely** — it was textbook BCI session-shift leakage.
2. **Z-score normalization** uses `X_train` statistics only; the same `mean`/`std` are applied to `X_test`.
3. **Pretrain data** (`dataset.prepare_pretrain_data`): session 1 of all 9 subjects. Session 2 of every subject stays held out.
4. **Test-time augmentation**: none. `evaluate()` is the only evaluation path, no TTA averaging.
5. **Output assertion**: every per-subject call prints `[no-leakage] Subject N: train=sessionX (n), test=sessionY (n); z-score from train only`.

## Model

`MTSEEGFormer` is the only model class. Five stages, all executed in a single forward pass:

1. **EEGNet-style spatial-temporal embedding** — 1x25 temporal conv + depthwise 22x1 spatial conv
2. **Progressive downsampling** — three stride-2 convs: 1000 → 500 → 250 → 125 timesteps
3. **Multi-scale temporal conv** — three parallel 1D convs (kernels 15/35/55) projecting back to 64
4. **Two DSTS blocks** — TCN (temporal) + pre-norm Transformer (over time tokens) summed with LayerNorm fusion
5. **Mean-pool over time + Linear classifier** to 4 classes

Total: 364K parameters, 22-channel input at 1000 timesteps (4s @ 250Hz).

## Training

One configuration for all 9 subjects (no per-subject tuning):

| Param | Value |
|---|---|
| Optimizer | AdamW (lr=1e-3, weight_decay=0.02) |
| Scheduler | OneCycleLR (max_lr=5e-3, 15% warmup, cosine) |
| Epochs | 500 (patience 150 for early stop) |
| Batch size | 64 |
| Label smoothing | 0.1 |
| MixUp | alpha=0.3, applied ~60% of batches |
| Augmentations | S&R, frequency masking, channel dropout, channel permutation, temporal shift, Gaussian noise |
| Pretrain | 500 epochs on session 1 of all 9 subjects (cross-subject, no leakage to test) |
| Single seed | 42 |
| Ensembling | None |
| TTA | None |

## Results (session-based, no leakage)

Per-subject test accuracy from `results.csv`:

| Subject | Accuracy |
|---|---|
| 1 | 86.11% |
| 2 | 60.76% |
| 3 | 90.97% |
| 4 | 82.99% |
| 5 | 71.53% |
| 6 | 64.58% |
| 7 | 89.24% |
| 8 | 82.29% |
| 9 | 85.76% |

**Mean: 79.28% ± 12.25%**

## Why 85% was hard (and what would close the gap)

The 5.72-point gap to the 85% target is concentrated in three "hard" subjects of BCI IV-2a (S2=58.7%, S5=70.5%, S6=64.6%). These subjects are well-known to be difficult across the literature — they have lower mu/beta amplitude and noisier motor-imagery patterns, so a single model trained on 288 trials of session-1 data struggles to generalize to session 2.

Closing the gap cleanly would likely require (any one of):
- **More pretraining data**: the current pretraining uses only session 1 of the 9 IV-2a subjects. Adding data from related datasets (e.g., BCI IV-2b, High Gamma) for self-supervised pretraining would help
- **Self-supervised pretraining**: masked autoencoding or contrastive pretraining on the unlabeled session-1 EEG (we have 2592 trials). The current pretraining is supervised cross-entropy
- **Bigger receptive field** for hard subjects, or a smaller-capacity model (current 364K params may be over-parameterized for 288 trials)

These were not attempted because they would either add data sources or change the "single architecture, no tricks" constraint. The reported 79.28% is the honest result of the requested setup.

## Requirements

- Python >= 3.12
- PyTorch >= 2.1.0 (CUDA recommended; tested on 4GB VRAM)
- MOABB >= 1.2.0
- MNE >= 1.8.0
- pyriemann >= 0.3

## Reproducibility

```bash
uv sync
uv run python main.py
```

First run: ~80 min on a 4GB GPU (50 min pretrain + ~30 min fine-tune). Subsequent runs: ~30 min (skip pretrain).
